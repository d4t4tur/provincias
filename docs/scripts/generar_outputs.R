source(file = "scripts/aereo.R", local = T)
detachAllPackages()
rm(list = ls())

source(file = "scripts/interno.R", local = T)
detachAllPackages()
rm(list = ls())

source(file = "scripts/prestadores_mapas.R", local = T)
detachAllPackages()
rm(list = ls())

source(file = "scripts/prestadores.R", local = T)
detachAllPackages()
rm(list = ls())

source(file = "scripts/receptivo.R", local = T)
detachAllPackages()
rm(list = ls())

source(file = "scripts/padron.R", local = T)
detachAllPackages()
rm(list = ls())

source(file = "scripts/parques.R", local = T)
detachAllPackages()
rm(list = ls())
